sudo: false
language: rust
cache: cargo

rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: beta
    - rust: nightly
  fast_finish: true

script: |
  cargo fmt --verbose -- --check &&
  cargo build --verbose &&
  cargo test --verbose &&
  cargo doc --verbose
